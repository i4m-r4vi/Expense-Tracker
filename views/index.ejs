<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Tracking</title>
    <link rel="shortcut icon" href="/money.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
</head>

<body>
    <div class="container">
        <h2 class="text-center my-4"><b>Expense Tracker</b></h2>
        <div class="data_c p-4">
            <div class="Amount_section">
                <h2 class="text-center"><b>Balance: </b>₹<%= balance %>
                </h2>
                <div>
                    <div class="d-flex text-center">
                        <section class="border p-4 rounded-2">
                            <h2>Income</h2>
                            <p class="text-success" id="income">₹ <%= income %>
                            </p>
                        </section>
                        <section class="border p-4 ms-3 rounded-2">
                            <h2>Expense</h2>
                            <p class="text-danger" id="expenses">₹ <%= expense%>
                            </p>
                        </section>
                    </div>
                    <div class="my-4">
                        <h2>New Transaction</h2>
                        <form action="/add-transaction" method="post">
                            <label for="expense" class="form-label">Expense Name</label><br>
                            <input type="text" name="expenseName" placeholder="Enter Name of Expense"
                                class="form-control" required><br>
                            <label for="amount" class="form-label">Amount</label><br>
                            <input type="text" name="amount" placeholder="Enter Amount" class="form-control"
                                required><br>
                            <div class="d-flex justify-content-end">
                                <button class="btn btn-primary" type="submit">Add Transaction</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="viewing_section">
                <h2>Transaction History</h2>
                <div class="text-light trans-h" id="history">
                    <% TransactionArray.forEach((Transaction)=>{%>
                        <form action="/delete" method="post" class="<%= Transaction.amount>0? 'bg-success': 'bg-danger' %>  py-2 rounded-2 m-2 d-flex">
                            <button class="btn_style ms-2"><i class="fa fa-trash"></i><input type="hidden" value="<%=Transaction._id %>" name="dele_amount"></button>
                            <p class="m-0 ms-2"><%= Transaction.name%> = <%= Transaction.amount%></p>
                        </form>       
                    <%})%>
                </div>
            </div>
        </div>
    </div>
</body>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</html>